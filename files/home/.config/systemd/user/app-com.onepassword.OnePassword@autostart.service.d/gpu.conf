# 1Password rewrites its own autostart .desktop on every start, so the flag
# cannot live there. This drop-in overrides the unit that
# systemd-xdg-autostart-generator builds from that file.
# See docs/electron-glitch.md.
[Service]
ExecStart=
ExecStart=/opt/1Password/1password --silent --disable-gpu-compositing
