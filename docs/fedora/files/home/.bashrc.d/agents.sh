# Omarchy agent aliases. "a" starts the default agent, "cx" starts Claude Code.
# This machine uses only Claude Code, so the 2 aliases are the same.
alias a='claude --permission-mode auto'
alias cx='claude --permission-mode auto'
